# Bikeapelago Development TODOs

- [ ] Implement database storage for GPX generation timestamps
- [ ] Implement logic to compare `.fit` file timestamps with `.gpx` timestamps in `SessionValidator`
- [ ] Implement logic to ensure .fit file wasn't completed impossibly quickly (for example maybe enforce a "speed limit" average of 25mph or something like that)
- [ ] Way of reverse geocoding nodes and displaying a friendly name for them throughout the UI
- [ ] Signal Amplifier item which increases the node-check radius from 30m to 250m for one ride. 
- [ ] Hint items